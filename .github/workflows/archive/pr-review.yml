name: PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    uses: ./.github/workflows/gemini-pr-review-v2.yml
    with:
      post_mode: "both"
      force_review: true
      min_lines_threshold: 1
    secrets: inherit
